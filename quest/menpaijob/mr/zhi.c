#include <ansi.h>
inherit ITEM;

void create()
{
        set_name(HIM"大燕传国玉玺"NOR, ({"mr yuxi"}));
        set("unit", "个");
        set("long", "大燕传国玉玺。\n");
        set("value", 0);
        setup();
}

