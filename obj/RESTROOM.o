// a skeleton for user rooms
inherit ROOM;
void create()
{
set("short", "SHORT_TITLE");
set("long", @LONG
LONG_TITLE
LONG
);
        set("exits", ([
]));
set("objects", ([
        "/p/npc/master":1,
       ]) );
	set("owner", "bigtie7001");
	set("class", "bigwiz7001");
	set("no_fight",1);
        set("no_clean_up", 1);
	set("sleep_room", "1");

setup();
}
